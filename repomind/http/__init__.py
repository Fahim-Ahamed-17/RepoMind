"""FastAPI layer over the library, for the local web UI (RM-090, M9).

Named as its own layer in docs/design.md section 2 (the "FastAPI" box) but
not yet built -- this stub exists only so the import-linter layers contract
in pyproject.toml is computable before M9 lands.
"""
